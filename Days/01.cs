namespace Days
{
    public static class First
    {
        public static string OutputFirst(string filename)
        {
            var sample = Helpers.ReadFileAsInteger(filename);
            return string.Empty;
        }

        public static string OutputSecond(string filename)
        {
            var sample = Helpers.ReadFileAsInteger(filename);
            return string.Empty;
        }
    }
}